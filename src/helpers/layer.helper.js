export default {
    fixedScreenLayers: null,
    /**
     * The data of fixed screen size before switching from fixed to responsive screen
     * @param {} layers 
     */
    setFixedData(layers) {
        this.fixedScreenLayers = layers;
    },

}